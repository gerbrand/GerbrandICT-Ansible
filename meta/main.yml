galaxy_info:
  author: Gerbrand van Dieyen
  description: Gerbrand's ansible playbook to make it easier to (re)install my laptop
  company: Gerbrand-ICT

  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_url: http://example.com/issue/tracker

  license: license GPLv3

  min_ansible_version: 2.2

  github_branch: master

  platforms:
  - name: Ubuntu
    versions:
    - xenial

  galaxy_tags: []
    # List tags for your role here, one per line. A tag is
    # a keyword that describes and categorizes the role.
    # Users find roles by searching for tags. Be sure to
    # remove the '[]' above if you add tags to this list.
    #
    # NOTE: A tag is limited to a single word comprised of
    # alphanumeric characters. Maximum 20 tags per role.

dependencies:
  - src: git:https://github.com/PlutusIt/plutus-ansible-roles.git
    name: plutus.roles

#  - { role: 'plutus-ansible-roles/owncloud' }
#  - { role: 'plutus-ansible-roles/mysql' }
#  - { role: 'plutus-ansible-roles/sbt' }
#  - { role: 'plutus-ansible-roles/ethereum' }
#  - { role: 'plutus-ansible-roles/mono' }
#  - { role: 'btsync' }
#  - { role: 'insync' }
